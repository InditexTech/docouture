# Selectors/values here were carried over verbatim from their original
# source (see dt-tokens.css's own header) rather than authored against this
# project's own style conventions, and stylelint-config-standard was never
# applied to them. Reformatting to satisfy it would be churn against values
# this project didn't choose in the first place.
src/css/dt-tokens.css
src/css/dt-breakpoints.css
src/css/dt-components.css
