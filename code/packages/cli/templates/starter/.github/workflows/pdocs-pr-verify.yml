name: pdocs-pr-verify

# Validates that the docs still build on a pull request. Deliberately builds
# only the PR's own HEAD — not whichever refs this site's versioning mode
# (see the docs-site-package skill's versioning-modes.md) publishes from:
#
#   - Mode 1 (Full History): not the `next`/prerelease branch, not any
#     `v*` release tag.
#   - Mode 2 (Stable + Prerelease): not the `prerelease` branch, not the
#     rolling `stable` tag.
#
# A PR checkout is on a detached HEAD or a feature branch — neither of those
# refs — so building the real `antora-playbook.yml` here would either fail
# (the refs it names are not reachable from a shallow, single-ref checkout)
# or silently validate the wrong content. `antora-playbook.pr-verify.yml`
# (see that file's own header) exists for exactly this: one content source,
# `branches: HEAD`, so this always validates whatever the PR actually
# changed — no `fetch-depth: 0` needed, since no other ref is ever read.
on:
  pull_request: {}

concurrency:
  group: pdocs-pr-verify-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          # Reads engines.node from package.json — no separate version file
          # to keep in sync in a standalone scaffolded repo.
          node-version-file: package.json
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npx antora --fetch antora-playbook.pr-verify.yml
