{{!--
SPDX-FileCopyrightText: 2026 INDUSTRIA DE DISEÑO TEXTIL S.A. (INDITEX S.A.)

SPDX-License-Identifier: MPL-2.0
--}}

<!DOCTYPE html>
<html lang="en"{{#if page.home}} data-page-home{{/if}}>
  <head>
{{> head defaultPageTitle='Untitled'}}
  </head>
  <body class="article{{#with (or page.attributes.role page.role)}} {{{this}}}{{/with}}">
{{> header}}
{{!-- GH-197: page-level overlay, present on every page a `.doc` body could hold a zoomable image/diagram (image-zoom-dialog.hbs's own header). --}}
{{> image-zoom-dialog}}
{{> body}}
{{> footer}}
{{!--
  GH-77: the site footer (brand, authored link columns, modules, copyright)
  now closes every doc page too, not just the landing — sits after the page
  footer's own previous/next + feedback row, same "outside .body, sibling of
  it" placement home.hbs already uses (site-footer.css's own margin-left
  handles the side-menu offset independently of .page-footer's identical
  rule, so the two stack with no conflict).
--}}
{{> site-footer}}
  </body>
</html>
