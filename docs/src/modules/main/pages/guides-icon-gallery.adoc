= Icon gallery

Every icon this site's UI bundle actually ships, so you never have to guess a
name and find out only after a card renders with a blank header slot.

The set below is fixed by whichever `@inditextech/docouture-ui-bundle` version this
site depends on — a normal site cannot add to it at request time, since the sprite is
baked into the published package. The icons themselves come from
https://lucide.dev[Lucide] (ISC licensed, public npm) — a name here is the same name
`lucide.dev/icons/<name>` uses, so browsing that catalogue is a legitimate way to find
a candidate before checking whether it's already vendored below. Need one that isn't?
Open a request against the `docouture` repository — vendoring a new Lucide icon is one
line in `packages/ui-bundle/src/img/icons.yml` plus a UI bundle release, but a site
consuming the published package can't do that itself.

== Two places an icon reference is read, two different rules

* `nav_modules[].icon` (see xref:main:guides-adding-a-module.adoc[]) is resolved by a
  Handlebars helper at *page-render* time, with full access to the icon sprite — any
  icon in this gallery works there, both sections below.
* `[card,icon="name"]` (see xref:main:reference-custom-asciidoc-components.adoc[]) is
  resolved by the Asciidoctor extension itself at *build* time, which has no access
  to the sprite at all — it can only emit a pre-generated CSS class. Only the icons in
  the first section below have one. Reference one from the second section instead,
  and nothing errors — the card just renders with an empty icon slot, silently.

== Usable in `[card,icon=]`

Every icon here has a generated CSS mask, so it's guaranteed to actually render in a
card's header. Copy the name as-is.

[cards]
====
[card,icon="circle-check-big",subheader="Copy button · admonition"]
.xref:main:reference-custom-asciidoc-components.adoc[circle-check-big]
The check that flashes after a successful code-block copy, and the `IMPORTANT`
admonition's icon.

[card,icon="code",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[code]
Vendored specifically as a `[card,icon=]` example.

[card,icon="triangle-alert",subheader="Admonition"]
.xref:main:reference-custom-asciidoc-components.adoc[triangle-alert]
The `WARNING` admonition's icon.

[card,icon="octagon-alert",subheader="Admonition"]
.xref:main:reference-custom-asciidoc-components.adoc[octagon-alert]
The `CAUTION` admonition's icon.

[card,icon="chevron-down",subheader="Accordion · nav switcher"]
.xref:main:reference-custom-asciidoc-components.adoc[chevron-down]
The disclosure marker on a `[%collapsible]` item, and the nav-switcher's own chevron.

[card,icon="file",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[file]
Vendored specifically as a `[card,icon=]` example.

[card,icon="sticky-note",subheader="Admonition"]
.xref:main:reference-custom-asciidoc-components.adoc[sticky-note]
The `NOTE` admonition's icon.

[card,icon="store",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[store]
Vendored specifically as a `[card,icon=]` example.

[card,icon="message-circle",subheader="Admonition"]
.xref:main:reference-custom-asciidoc-components.adoc[message-circle]
The `TIP` admonition's icon.

[card,icon="external-link",subheader="Feature tabs · hero"]
.xref:main:reference-custom-asciidoc-components.adoc[external-link]
Auto-appended to a `[feature-tabs]` slide's `[.cta]` link, and to a hero action
button, whenever the target isn't a resolvable internal page.

[card,icon="grid-3x3",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[grid-3x3]
The icon on this very site's own Architecture quicklink card, and on `main`'s
`nav_modules` entry — proof the same string works in both places.

[card,icon="server",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[server]
Vendored specifically as a `[card,icon=]` example.

[card,icon="flame",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[flame]
Vendored specifically as a `[card,icon=]` example.

[card,icon="shapes",subheader="Card icon example"]
.xref:main:reference-custom-asciidoc-components.adoc[shapes]
Vendored specifically as a `[card,icon=]` example.
====

== Vendored, but not card-safe

These exist in the sprite and work fine in `nav_modules[].icon`, but have no CSS mask
— putting one in `[card,icon=]` renders no icon and no warning.

[cols="1,2"]
|===
|Reference |Where it's actually used

|`moon`
|Dark-mode icon on the light/dark theme toggle.

|`plus`
|Vendored; no current template use in this bundle version.

|`arrow-left`
|Vendored; no current template use in this bundle version.

|`x`
|Side-menu drawer's close button (mobile breakpoint).

|`x`
|A recent-search entry's remove button.

|`copy`
|A code block's copy-to-clipboard button, idle state.

|`maximize`
|A code block's "view full screen" button.

|`minimize`
|The same button, swapped in once full screen is active.

|`search`
|The header toolbar's search trigger, and the search dialog itself.

|`arrow-left`
|Pagination's "previous page" link.

|`arrow-right`
|Pagination's "next page" link.

|`chevron-up`
|Vendored; no current template use in this bundle version.

|`cloud`
|A `nav_modules[].icon` example (a "Store"-style module).

|`link`
|A section heading's anchor / copy-link button.

|`wifi`
|A `nav_modules[].icon` example (a "Websockets"-style module).

|`circle-play`
|Vendored; no current template use in this bundle version.

|`menu`
|The side-menu's open/close toggle.

|`panel-left`
|The "on this page" table-of-contents toggle.

|`wrench`
|A `nav_modules[].icon` example (a "React Helper"-style module).

|`house`
|Vendored; no current template use in this bundle version.

|`rotate-ccw-clock`
|The "recent searches" list heading.

|`sun`
|Light-mode icon on the light/dark theme toggle.
|===

See xref:main:guides-adding-a-module.adoc[] for `nav_modules[].icon` syntax and
xref:main:reference-custom-asciidoc-components.adoc[] for `[card,icon=]` syntax.
