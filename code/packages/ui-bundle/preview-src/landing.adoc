= Brand&#8217;s Hardware &amp; Software Requirements
Author Name
:idprefix:
:idseparator: -
:page-pagination:
:page-role: -hero
:description: Landing test page: the site chrome around a normal doc page — header, side menu, page-versions, theme toggle, edit-this-page, pagination and footer — opted out of the hero (`:page-role: -hero`) so this branch gets exercised too; see content.html for the hero on.

[.lead]
This is the *landing* test page.
It renders through the same `default` layout as link:content.html[the content test page], so the diff between the two is entirely in `preview-src/ui-model.yml` and this page's own `:page-role: -hero`: this page is `page.home: true`, so the header's product-name link should show as current and the search field autofocuses.

Everything else on this page is here to exercise site chrome rather than article content: the header toolbar, the left-hand nav tree, the right-hand table of contents, and the previous/next pagination in the footer.

== Cu solet

Nominavi luptatum eos, an vim hinc philosophia intellegebat.
Eu mea inani iriure.

== Liber recusabo

Select menu:File[Open Project] to open the project in your IDE.

=== Some Code

[,js]
----
const chrome = 'header + toolbar + nav + toc + pagination + footer'
----
